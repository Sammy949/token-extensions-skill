# Local working notes — never pushed
notes/

# Local verification harness — run to prove recipes, promoted to examples/ once green
verify/

# OS / editor cruft
.DS_Store
*.swp
.vscode/
.idea/

# deps / build (in case we add a TS test project later)
node_modules/
dist/
target/
.env
.env.*
