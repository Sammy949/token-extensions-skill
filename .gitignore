# Local working notes — never pushed
notes/

# Local verification harness — run to prove recipes, promoted to examples/ once green
verify/

# OS / editor cruft
.DS_Store
*.swp
.vscode/
.idea/

# deps / build
node_modules/
dist/
target/
.env
.env.*

# local validator ledger (created by `solana-test-validator -r`)
test-ledger/
